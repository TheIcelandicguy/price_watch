# --- Secrets / local state (NEVER commit) ---
# Local backups may contain leaked API keys and real .storage dumps
.backups/
*.secret
secrets.yaml

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Prior chat transcript export (bulky, may contain pasted keys)
This is a copy of a chat between Claude and Davíð.txt
*.transcript.txt

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/

# --- Frontend (panel) ---
panel/node_modules/
panel/*.tsbuildinfo

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
