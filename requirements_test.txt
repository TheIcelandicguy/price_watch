pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-homeassistant-custom-component>=0.13
respx>=0.21
# Integration runtime deps that are imported at module load, so the test
# env needs them just to collect (see manifest.json "requirements").
anthropic>=0.40.0
openai>=1.40.0
beautifulsoup4>=4.12.0
httpx>=0.27.0
